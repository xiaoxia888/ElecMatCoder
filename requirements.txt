# 分词模块依赖
jieba>=0.42.1
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0

# API服务依赖
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# LLM预标注模块依赖（可选）
httpx>=0.25.0
tqdm>=4.65.0

# NER模型训练依赖
torch>=2.0.0
transformers>=4.36.0
pytorch-crf>=0.7.2
numpy>=1.24.0

# 语义相似度匹配依赖（材料编码模块）
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4  # 如果有GPU可使用 faiss-gpu

# Excel处理依赖
pandas>=2.0.0
openpyxl>=3.1.0

# 开发依赖
# pytest>=7.0.0
# pytest-asyncio>=0.21.0




  bert训练任务环境
pip install torch==2.9.1 \
  transformers==5.0.0 \
  accelerate==1.12.0 \
  numpy==2.4.0 \
  pandas==2.3.3 \
  scikit-learn==1.8.0 \
  tqdm==4.67.1 \
  PyYAML==6.0.3 \
  matplotlib==3.10.8