workers:
  type_worker:
    base_url: "http://127.0.0.1:8201"
    timeout_seconds: 300
    models:
      - "type"

  material_worker:
    base_url: "http://127.0.0.1:8202"
    timeout_seconds: 300
    models:
      - "material-standard"

gateway:
  concurrency_mode: "serial"
