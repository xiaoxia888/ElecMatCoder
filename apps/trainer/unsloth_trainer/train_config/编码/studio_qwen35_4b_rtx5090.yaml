# Unsloth Studio: 编码 / Qwen3.5-4B / RTX 5090 / BF16 LoRA
# 实际数据：train=4415，val=549；总文本最大 166 字符，输出中位数仅 5 字符。
# 有效 batch 与 4090 保持为 16。尾批次规避目标：补齐 1 条到 4416。
# Studio 页面选择：Qwen3.5-4B、LoRA、Alpaca、coder_stage2_train.json、coder_stage2_val.json。

training:
  max_seq_length: 256
  num_epochs: 3
  learning_rate: 0.00002
  embedding_learning_rate: null
  batch_size: 16
  gradient_accumulation_steps: 1  # 有效 batch=16
  warmup_steps: 25
  max_steps: 0
  save_steps: 50
  eval_steps: 50
  weight_decay: 0.01
  random_seed: 2026
  packing: false
  train_on_completions: true
  gradient_checkpointing: unsloth
  optim: adamw_8bit
  lr_scheduler_type: cosine

lora:
  lora_r: 16
  lora_alpha: 32
  lora_dropout: 0
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  use_rslora: false
  use_loftq: false
  use_dora: false

logging:
  enable_wandb: false
  wandb_project: field-coding
  enable_tensorboard: false
  tensorboard_dir: runs/coder/rtx5090
  log_frequency: 10
