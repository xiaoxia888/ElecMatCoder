# 推荐优先使用同目录 environment-linux.yml 创建隔离的 Conda 环境。
# 不要在这里单独固定 torch；vLLM wheel 会安装与其匹配的版本。
vllm==0.26.0
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
pydantic>=2.9,<3
requests>=2.32,<3
PyYAML>=6.0,<7
openpyxl>=3.1,<4
pytest>=8,<9
