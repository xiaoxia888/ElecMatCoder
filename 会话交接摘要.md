# ElecMatCoder 会话交接摘要

> 生成时间：2026-08-18（Asia/Shanghai）  
> 用途：在新 Codex 会话中恢复项目上下文。请先阅读本文，再查看 `git status` 和目标文件。  
> 原会话 ID：`019f3549-9210-79d2-95c6-d38a99b26dff`

## 1. 最重要的交接说明

1. 当前仓库不是干净工作区。生成本文前共有 **28 个修改文件、5 个删除文件、13 个未跟踪项**，其中包含大量数据集和正在开发的代码。不要执行 `git reset --hard`、`git clean` 或覆盖式回退。
2. 当前分支为 `main`，`HEAD` 与 `origin/main` 都是 `287aa9a change：尺寸新结构适配以及调用方式优化`。工作区中的大量改动尚未提交。
3. 原会话没有丢失，本地归档仍在：
   `/Users/guoxi/.codex/sessions/2026/07/06/rollout-2026-07-06T10-37-21-019f3549-9210-79d2-95c6-d38a99b26dff.jsonl`
   本轮核验为 **66,104 行**。不要修改该文件，可将其作为只读证据源。
4. 原会话后期因上下文极大且经历多次压缩，普通请求和远程压缩都出现 `Request blocked.`。相同的正常问题在新会话可以回答，因此更像旧会话上下文触发的误拦截，而不是用户问题本身违规；错误记录只给出 `other`，无法进一步确定服务端规则。
5. 本文不保存密码、令牌或其他凭据。若后续工作需要凭据，应由用户重新确认，不要从旧日志复制。

## 2. 项目目标与主链路

项目处理工业材料描述，核心链路可以概括为：

`原始材料描述 → 一阶段模型抽取字段 → 确定性编码器 → 二阶段表面证据/分流检查 → 必要时 coder 兜底 → 最终 C1 编码`

当前主要工作线包括：

- 按任务构造和审核 SFT 数据集：种类、材质规范、尺寸/壁厚/磅级、编码等。
- 使用 Qwen3/Qwen3.5 基座与 LoRA 做监督微调。
- 用确定性编码规则约束和补充模型结果。
- 对简单/困难分流、模型置信分和自动放行风险做分析。
- 对多个模型在同一 Excel 数据上进行可复现对比评测。

## 3. 已确定的训练方案

### 3.1 为什么选 SFT

材料描述到结构化字段属于“输入—标准答案”边界清晰、输出格式固定的映射任务。SFT 能直接利用已审核标注，使模型学习行业术语、字段边界和 JSON 输出规范；相较只依赖提示词，它更容易提升准确率、一致性和可控性，也便于按训练集/验证集量化迭代。当前阶段目标是稳定复现专家标注，不是开放式偏好优化，因此先采用 SFT 更合适。

### 3.2 为什么用 Alpaca 格式

Alpaca 的 `instruction`、`input`、`output` 三段式结构正好对应“任务说明、材料描述、标准结构化答案”。它结构简单、便于人工审核，能被 LLaMA Factory 和当前独立 Unsloth 模块直接读取，也方便多个任务复用统一的数据处理流程。

可直接用于会议汇报的简短表述：

> 本项目采用监督微调（SFT），是因为材料描述解析属于目标明确、答案可标注的结构化抽取任务。通过高质量样本直接学习行业术语、字段边界及标准 JSON 输出，可显著增强模型的准确性、一致性和可控性。训练数据使用 Alpaca 格式，将任务指令、原始材料描述和标准答案分别组织为 instruction、input、output，结构清晰，便于审核、扩展，并可兼容现有训练框架。

### 3.3 独立 Unsloth Core 方案

目录：`apps/trainer/unsloth_trainer/`

- 独立于 LLaMA Factory 和 Unsloth Studio，直接使用 `FastLanguageModel`、`SFTTrainer`、`SFTConfig`。
- 公共配置在 `config.yaml`，任务差异放在 `train_config/**/*.yaml`，采用深度合并。
- 支持 Alpaca、ChatML 和纯文本；默认只对 assistant 回答计算 loss。
- 默认 BF16 LoRA，`r=16`、`alpha=32`，训练自动创建时间戳目录。
- `resume_from_checkpoint: auto` 会恢复最新有效 checkpoint；检测到完整最终 adapter 后拒绝重复训练。
- 稳定环境固定为 Python 3.12、PyTorch 2.8.0/cu128、Triton 3.4.0、Transformers 5.3.0、`unsloth[cu128-torch280]==2026.8.16`、`unsloth-zoo==2026.8.11`。
- Qwen3.5-9B / RTX 5090 配置：`train_config/种类/qwen35_9b_rtx5090.yaml`，BF16 LoRA，`max_seq_length=1024`，物理 batch 1、梯度累积 16、2 epochs。

服务器若只复制了 `/home/waas/unsloth_trainer`，从其父目录执行：

```bash
python -m unsloth_trainer.train \
  --config unsloth_trainer/train_config/种类/qwen35_9b_rtx5090.yaml \
  --validate-only
```

完整仓库布局则执行：

```bash
python -m apps.trainer.unsloth_trainer.train \
  --config apps/trainer/unsloth_trainer/train_config/种类/qwen35_9b_rtx5090.yaml \
  --validate-only
```

正式训练前应先将 `max_steps` 临时设为 `20` 做冒烟测试，成功后恢复为 `-1`。

### 3.4 训练故障的既有判断

旧会话中 Qwen3.5-4B/9B 在 LLaMA Factory/Studio 出现过 `NaN`、CUDA illegal memory access，以及首轮末尾 `max(): input.numel()==0`。数据检查未发现空答案，样本长度也未超过 1024；主要怀疑是 Python 3.13、Torch/cu130、Triton、FLA/快速内核与本机 CUDA 12.8 组合不稳定。`nvidia-smi` 显示的 CUDA Version 只是驱动支持上限，不等于当前 Python 环境实际使用的 toolkit。既定建议是使用上述固定版本的独立 Unsloth Core 环境，不再用 Studio 环境直接训练。

## 4. 数据集现状

### 4.1 当前已核验规模

| 任务 | 训练集 | 验证集 | 当前来源/说明 |
|---|---:|---:|---|
| 种类 | 24,145 | 2,031 | `按8类拆分llamafactory数据集/种类/0806/` |
| 材质规范 | 69,719 | 7,749 | `按8类拆分llamafactory数据集/材质规范/0814/`；结构化原始牌号文件数量相同 |
| 尺寸 V2 主文件 | 49,996 | 5,602 行 | 当前磁盘实数；验证集只有 5,540 个唯一 input，见下一节 |
| 尺寸 V2 专项增强 | 698 | 62 | 训练仅合成增强；验证仅真实项目错误 |
| 编码 | 4,415 | 549 | 来自训练配置注释，本文未重新定位原始文件计数 |

### 4.2 尺寸 V2：当前必须先解决的一致性问题

路径：`apps/trainer/qwen3_fte/output/按8类拆分数据集/尺寸壁厚磅级/`

基础划分报告仍记录：

- 总数 54,776；训练 49,298；验证 5,478；验证占比 10.0007%。
- 骨架交叉 0，归一化 input 泄漏 0。
- 结构、表达形式、实际值的加权平均偏差分别为 0.367、0.169、1.150 个百分点。

但当前主文件实际为：

- `尺寸壁厚磅级V2_train.json`：49,996 行、49,996 个唯一 input。
- `尺寸壁厚磅级V2_val.json`：5,602 行、5,540 个唯一 input。
- 训练/验证唯一 input 交叉仍为 0。
- 698 条专项合成训练样本已各合并一次进入主训练集。
- 62 条专项真实错误验证样本在主验证集里各出现 **2 次**，因此多出 62 个重复行。
- `尺寸壁厚磅级V2_划分报告.json` 没有随主文件更新，不能再直接代表当前主数据。
- 尺寸训练配置注释仍写 `train=49298，val=5540`，也没有反映当前主文件的 49,996/5,602 行。

继续训练前建议先确认预期口径：专项增强是否应该合并入主数据。如果应该合并，应将验证集 62 组重复各去掉一份，使合并后规模为 **49,996/5,540**，再重新生成一致的报告并更新配置注释；如果不应合并，则恢复主文件为基础划分并单独进行专项对比。未获得用户确认前不要擅自改数据。

### 4.3 尺寸 V2 专项增强集

目录：`V2专项增强对比/`

- 共生成 760 条：训练 698、验证 62。
- 训练来源全部为 `synthetic_contrast`，验证来源全部为 `actual_project_error`。
- 与原 54,776 条 V2 描述交叉 0，训练/验证描述交叉 0，对比组交叉 0。
- 跳过 11 条专项内部重复、25 条与原 V2 重叠的候选。
- 压力表达 `150 Lbs`、`150#`、`Class 150` 统一标为 `CL150`。
- 已移除 `设计压力：6Bar` 之类口径未确认的训练表达。
- 暂不处理：OD/INCH 到 DN 的换算标签策略、一阶段压力答案与最终 C/CL 编码差异、二阶段编码/平台后处理错误、非等级设计压力。
- 仅报告但未自动改主数据：341 条紧凑双尺寸疑似未形成双位置标签，63 条疑似 OCR 第二端表达。

生成脚本：`apps/trainer/qwen3_fte/src/generate_size_v2_error_contrast_dataset.py`

## 5. 规则、编码与二阶段检查的既有决策

### 5.1 分流口径

- 用户明确否定“缺尺寸、壁厚或压力就一律困难”的结构完整性规则；字段缺失本身不能单独决定困难。
- 难度可理解为：困难（0）、简单（1）、二次简单/需复核（2）。一阶段命中任一硬规则可判困难；二阶段大多用于“简单但需复核”，具体仍以配置和代码为准。
- 报告时必须把“仅分流”与“分流 + 置信分交叉”分开，不要用空泛结论代替数据。

### 5.2 当前未提交的规则方向

相关文件集中在：

- `src/encoder/processors/structural_numeric_rules.py`
- `src/encoder/processors/structural_rule_config.py`
- `src/material_description_splitter/second_pass/source_evidence_completeness_checker.py`
- `src/material_description_splitter/second_pass/*_surface_matcher.py`
- `src/encoder/pipe_encoder.py`

主要设计：

- 来源证据完整性检查只比较 SIZE、THICKNESS、PRESSURE 是否漏提，不推断 MAIN/BRANCH/END 等角色，也不做 TEE/REDUC/OLET 语义推断。
- 只采信较强的语法锚点、前缀、单位和组合形式；宁可漏掉含糊表达，也不把标准号、材质数字等误判成尺寸/壁厚。
- `common_dn_values` 与 OD/壁厚比例规则统一从共享配置读取，避免多个模块各自维护魔法数字。
- 自适应边界：字母边只排除相邻字母，数字边只排除相邻数字；目标是允许 `B16.5XS`、`5DN50X`、`5CL150RF`，同时拒绝 `ADN50`、`DN500`、`ACL150`、`CL1500`。
- 典型判定：`Φ88.9×60.3` 为双 OD；`Φ88.9×5.6` 为 OD + 毫米壁厚；显式 `mm` 优先；`Φ450×15` 会结合常见 DN 配置判断双尺寸。
- 同一位置存在多个同优先级壁厚时，编码器保留不同值，不再只取第一个。
- 直管同时存在壁厚和磅级时，当前未提交逻辑在最终编码中只保留壁厚，并在 notes 记录原因。
- 压力证据新增 MPa/bar 与 PN 的等价匹配，以及 LBS/# 等 Class 表达。
- 标准后缀允许紧邻标准年代，例如 `HG/T20592-2009B`；材质牌号的 I/II/III 后缀会被消费，避免被误当标准后缀。
- 材质表面匹配曾扩展到带字母的三位以上别名，并采用更长、更完整牌号优先，避免 `F316` 截断 `F316L`；独立出现的短牌号仍应算真实冲突。

这些都是工作区中的未提交改动，继续修改前应先阅读 diff 和相关测试。

## 6. 已新增的独立工具

### 6.1 多模型 Excel 评测

目录：`apps/model_excel_evaluator/`

- 按顺序加载 Qwen3-8B、Qwen3.5-4B、Qwen3.5-9B，避免三个模型同时占显存。
- 同一模型内部按共享 `inference.batch_size` 批量推理；RTX 5090 建议从 4 开始，不足时降为 2 或 1。
- 一阶段输出 JSON 后调用现有确定性编码器；规则失败时才用单独 coder 模型兜底。
- `--validate-only` 只校验配置、路径和 Excel 表头，不加载模型。
- 输出 `评测结果.xlsx`、`准确率对比.png`、`耗时对比.png`、`模型差异.png`；Excel 含明细、汇总、差异和运行配置四个工作表。

示例：

```bash
python -m apps.model_excel_evaluator.evaluate \
  --task type \
  --input /path/to/input.xlsx \
  --output-dir /path/to/output \
  --models qwen3-8B qwen3.5-4B qwen3.5-9B
```

注意：旧服务器曾把该目录复制到 `apps/trainer/model_excel_evaluator/`，那里的模块命令需要按实际部署路径调整；本地正式目录是 `apps/model_excel_evaluator/`。

### 6.2 分流与置信分分析

目录：`apps/trainer/routing_confidence_analyzer/`

该工具不调用模型，只读取已有 Excel，分析编码正确性、简单/困难分流、模型置信分及其交集。输出一个分析工作簿、一份文本结论和 4 张核心图。

旧会话对 24,914 条法兰数据的结果（本轮没有原始 Excel，未重新计算）：

| 策略（阈值 0.99） | 覆盖率 | 放行准确率 | 错误放行 | 错误拦截率 |
|---|---:|---:|---:|---:|
| 仅分流 | 70.94% | 99.08% | 162 | 87.29% |
| 仅置信分 | 88.81% | 98.54% | 324 | 74.59% |
| 分流 ∩ 置信分 | 67.83% | 99.40% | 102 | 92.00% |

历史结论：0.99 下仅置信分不够安全；交集策略比仅分流少放行 60 个错误，代价是覆盖率下降 3.11 个百分点。若目标放行准确率为 99.5%，旧分析建议交集阈值约 0.998、覆盖率约 61.29%。正式汇报前若原始 Excel 已更新，必须重新运行，不要直接复用这些数字。

## 7. vLLM 服务的未提交方向

- `service.yaml` 新增 Qwen3.5-9B engine 模板和四个 LoRA 路径占位。
- Profile 校验改为只要求当前 `models` 实际引用的 engine，加载后也会过滤未使用 engine。
- dual-3090/dual-4090 profile 和测试有同步修改。
- 服务器路径仍是示例，部署前必须替换成实际模型与 adapter 路径并运行配置测试。

## 8. 当前验证状态

2026-08-18 本轮实际执行：

- `apps.trainer.unsloth_trainer.tests.test_config_and_data`
- `apps.model_excel_evaluator.tests.test_evaluate`
- `apps.trainer.routing_confidence_analyzer.tests.test_analyze`

合计 **17 个 unittest 通过**。同时对训练器、评测器、分析器、encoder processors、second-pass 和专项数据生成脚本执行了 Python 语法编译检查，均通过。

本机当前 Python 环境没有安装 `pytest`，所以 `tests/` 下采用 pytest 写法的结构规则测试本轮没有执行。旧会话曾报告相关规则测试通过，但这不等于当前工作区已重新验证。后续应在项目测试环境中补跑，至少包括：

```text
tests/test_source_evidence_completeness_checker.py
tests/test_structural_second_pass_adaptive_boundaries.py
tests/test_standard_suffix_cross_field_consumption.py
tests/test_standard_edition_suffix_matching.py
tests/test_pipe_encoder_assembly_rules.py
tests/test_generate_size_v2_error_contrast_dataset.py
tests/test_structural_v2_runtime.py
apps/vllm_service/tests/test_config.py
```

## 9. 建议的继续顺序

1. 先运行 `git status --short`，阅读当前 diff，确认哪些改动属于本轮目标；不要清理用户改动。
2. 优先让用户确认尺寸专项增强是否合并进主训练/验证集。
3. 按确认结果消除验证集的 62 个重复行，并重新生成与实际文件一致的划分/合并报告和训练配置注释。
4. 在带 pytest 的项目环境补跑结构规则、encoder 与 vLLM 配置测试；失败时先定位，不要直接大范围改规则。
5. 在服务器用 `--validate-only` 校验 Unsloth 配置，再进行 `max_steps=20` 冒烟训练，最后才启动完整训练。
6. 若要做会议汇报，先确认评测 Excel 和分析结果是否为最新版本，再引用第 6.2 节数据。
7. 将相互独立的工作分批提交：数据集、结构规则/encoder、Unsloth 训练器、模型评测器、分流分析器、vLLM 配置，避免把大数据和多套代码混成一个提交。

## 10. 最近已提交的背景

最近主线提交依次覆盖：置信度优化、分流难度、难度与置信分分析、尺寸模型优化、配置，以及尺寸新结构适配和调用方式优化。当前 `HEAD`：

```text
287aa9a change：尺寸新结构适配以及调用方式优化
4af27ef 尺寸模型
a0310c6 配置文件
ded58f0 change：尺寸模型优化
3073183 分析难度和置信分
2ceb930 change：分流难度
68e1102 chaneg：置信度优化
```

---

给新会话的建议开场语：

> 请先阅读仓库根目录的 `会话交接摘要.md`，再运行 `git status --short`。保留所有现有改动，不要 reset 或 clean。首先核验尺寸 V2 主验证集的 62 组重复和过期划分报告，然后告诉我你准备怎样处理；未得到确认前不要修改数据集。
